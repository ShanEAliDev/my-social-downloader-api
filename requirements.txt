fastapi
uvicorn[standard]
# [default] pulls in yt-dlp's EJS challenge-solver scripts as a bundled
# Python dependency. Without this extra, yt-dlp needs those scripts fetched
# separately at runtime (--remote-components ejs:github/npm) - simpler to
# just have them included from the start.
yt-dlp[default]
slowapi
python-multipart
imageio-ffmpeg